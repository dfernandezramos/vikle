<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Vikle.UI"
             x:Class="Vikle.UI.Views.SecondaryHeader">
    <ContentView.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <local:Resources />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </ContentView.Resources>
    <Grid RowSpacing="0" ColumnSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackLayout Grid.Row="0"
                     HeightRequest="75">
            <StackLayout BackgroundColor="{StaticResource BlueStrongColor}" HeightRequest="50"/>
        </StackLayout>
        <StackLayout Grid.Row="0" BackgroundColor="{StaticResource YellowStrongColor}" HeightRequest="50" Margin="50,25,50,0">
            <Label Text="1234 ABC"
                   HorizontalOptions="CenterAndExpand"
                   VerticalOptions="CenterAndExpand"
                   FontAttributes="Bold"
                   FontSize="28"
                   TextColor="{StaticResource BlueMediumColor}"/>
        </StackLayout>
    </Grid>
</ContentView>